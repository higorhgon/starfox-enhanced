pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'StarFoxEnhanced'
include ':app'
// Opt-in standalone Quest APK; the ordinary Android app is unchanged.
include ':quest'
project(':quest').projectDir = file('../quest')
